[ce que jai eu le temps de faire]
*
chronometre des fonctions, la fonction CalculateTime renvoie un long long qui correspond au temps en microsecondes(dans le meme fichier cpp que main).
Faut lui envoyer le graphe + indiquer via l'enum quelle fonction appliquer.

Ces fonctions sont définies dans le meme et prennent en parametre le graphe (resolutionExacte/Approche  // matriceAdjacence) : 
On peut décider de juste afficher le résultat, où de le stoquer dans un fichier si on décide de faire plusieurs tests.



*
Resolution exacte, j'ai suivi le premier algo donné par le document "AlgoExact(fr)".   JE NAI PAS TESTE LE RESULTAT, peut aussi y avoir des bugs de conception.



[remarques au pif]

fonction : --generateGraph----
n/p : kesako? Nom + explcites
p pas utilisé dans la fonction

Gaffe aux fuites mémoire! J'ai implémenté le destructeur de graphe, pas sur que ça suffise. 